        <header>
            <div class="container">
                <div class="row">
                    <div class="col-md-12 head-bg">
                        <div class="content d-flex flex-column justify-content-center">
                            <img src="{{asset('images/logo.png')}}" alt="{{$title}}" class="img-fluid logo">
                            <p class="slogan">{{$slogan}}</p>
                        </div>
                    </div>
                </div>
            </div>
        </header>